# 复制为 .env 并按需填写（仅在「重新生成 data/」或「自定义托管基址」时需要）。
# .env 已被 .gitignore 忽略，绝不要提交 API Key。

# ── 托管基址：组装 dist/ 时写入书签与安装页的数据地址（需开启 CORS）。
# 默认 http://localhost:8080。GitHub Pages 工作流会自动设为你的 Pages 地址。
# M3E_HOST_BASE=https://你的用户名.github.io/mPython3-IDE-Enhanced

# ── 以下仅在 `bun run build:catalog` / `build:knowledge`（重新生成 data/）时需要。
# 这些原始导出不随仓库分发；默认指向项目内的 ./vendor/ 子目录。
# 逆向得到的站点 bundle 目录（含 site/js/app.*.js 与 extension_catalog_all.json）
# M3E_REVERSE_DIR=./vendor/reverse-online-mpython-cn
# 官方 Blockly 积木定义导出目录（含 i18n/ 与 non-strict/）
# M3E_BLOCK_EXPORT_DIR=./vendor/mpython-block-export
# HandPy 技能文档目录（含 references/）
# M3E_HANDPY_SKILL_DIR=./vendor/handpy-skill

# ── 运行 T3 LLM eval（`bun run eval`）时需要：
# M3E_API_KEY=sk-...
